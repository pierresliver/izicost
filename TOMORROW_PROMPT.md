# Prompt for the next IziCost session

Copy everything below the line into the chat to start.

---

We are continuing IziCost (folder D:\AI\Claude\Projects\izicost, one project per session, nothing shared with IziCamera).

Start by reading MASTER_PLAN.md section 12 ("Next steps"), especially the "Day 4" and "Day 3" blocks, so you know what exists. In short: Phases 1–4 of the app (camera + multi-photo + offline queue, reports/budgets/recap, community prices, basket optimiser + alerts), migrations 001–026 applied, Edge Functions `extract-receipt`, `parse-shopping-list` and `read-shelf` deployed with per-user and global daily caps, and everything from Days 2–3: voice shopping list (PT/EN), bargain-first Home, household sharing, "My items" price watch with background alerts, animated charts, inflation and city price index, share cards, open data in the app, weekly story, badges, Update button (public `releases` bucket, `node scripts/publish-release.js <apk>`), radius chooser, several shopping lists with merge, typical prices / estimated totals, shared household lists, Buy again, list-wide alerts, store pages.

Day 4 (2026-09-04 afternoon, see the "Day 4" block) added, NOT yet built into an APK:
- **Shelf scan** (tester-only): Scan tab → "Shelf scan" card → pick the shop (GPS / search / create), pick the interval, put the phone in a pocket; it takes a silent photo every N seconds with the screen dark; tap to pause; blurry photos are flagged; the kept ones are read by `read-shelf` in batches of 12; you check every price and publish. Only accounts in `trusted_seeders` can use it (`node scripts/trust-seeder.js <email or id prefix>`; the three real accounts — my household — are flagged). Guest sessions are refused. To open it to everyone later: `update public.community_settings set value = '1' where key = 'shelf_scan_open'`.
- **Brands**: every product has a brand (when recognisable) and a generic key; every shopping-list line has a brand chip ("Any brand" or a brand, with a picker showing the cheapest price per brand); the basket result prices the cheapest brand per shop; product pages have a "Compare brands" card; saying "leite parmalat" keeps to Parmalat.

Builds: build 6 = builds\izicost-v0.4.1-2026-09-04-1206.apk (0.4.1 / versionCode 6) is on my phone; it does NOT contain Day 4. Rule: **never start an APK build without my explicit yes in this session.** When I say yes: bump version to 0.5.0 / versionCode 7 in app\app.json, run `powershell -ExecutionPolicy Bypass -File scripts\build-apk.ps1`, copy to builds\, tell me the file name; then ALSO make the slim download (`scripts\build-apk.ps1 -Abi arm64-v8a -Slim`, then `node scripts/publish-release.js <slim apk> "notes"`) so the in-app Update button and the invite link work (Free plan caps files at 50 MB; the slim build fits).

Today:
1. I will give you my findings from testing build 6 (and build 7 once made). Fix them all. Ask for screenshots if something is unclear.
2. Tell me which of the three account ids (280099df / 992a80f4 / add533b1) is mine when I give you my email; then move my 5 old guest receipts (guest d36d289a, 2026-09-02) to my account on the server and check Home fills up.
3. Shelf scan first real test (my phone, build 7): walk one aisle, check the photo screen and the prices read; note what the reader gets wrong (we may tune the prompt), and whether the "photo N" reference is useful (tapping it does nothing yet).
4. Visual polish pass, screen by screen: consistent spacing, brand green, clean cards, dark mode correct, nice empty states. The app must look better than the competitor apps in research/COMPETITOR_RESEARCH.md section 3. The dashboard must feel fun, dynamic and interactive.
5. Seeding plan for the price pool (tester group with Shelf scan, promotion flyers, etc.). Decide with me.
6. Check the background price alerts really fire on my phone with the app closed; if unreliable, plan real push (Firebase).
7. Then continue the pre-launch security checklist in MASTER_PLAN section 12 (still open: captcha on guest sign-up, `min_reports` back to 2 before public launch, email confirmation back on, permanent signing keystore, stores insert validation, export/delete account, privacy policy, backups, final review). Also 9 pre-existing lint style errors in older files (setState-in-effect) could be cleaned up.

Rules as always: I don't code, explain in plain language, double-check everything you build (typecheck `npx tsc --noEmit` in app\, `npx expo lint`, `python scripts/i18n-audit.py`, and review + security agents for anything big — 110% security), keep English + Portuguese complete, never put secrets in the app or in git, commit and push at the end (git add -A, then commit, then push as separate commands — the classifier blocks them when combined).

Helpful facts: Supabase project ref ehomstenpqngybtemtad, keys in passwords\supabase.txt (never print them), migrations via `node scripts/setup-supabase.js sql <file>`, function deploy via `node scripts/deploy-function.js [name]`, backend smoke test via `node scripts/test-backend.js`, ad-hoc read-only SQL via a small script that posts to the management API (see how setup-supabase.js authenticates). Demo data is still in the database (`node scripts/seed-demo.js clean` removes it). My Expo Go is v54 and the project is SDK 57, so testing is by APK only. The Supabase access token expires around 2 October 2026. The invite message links to the latest published APK in the public `releases` bucket (`APP_LINK` in app\src\features\share\share.ts); replace it with the Play Store link once listed. If typed routes complain about a new screen, run `npx expo start --offline --port 8099` for a minute to regenerate `.expo/types/router.d.ts`.
